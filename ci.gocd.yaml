mypipe:
  group: mygroup
  materials:
    mygit:
      git: https://github.com/funfuck/scg-web.git
  stages:
    - build:
        jobs:
          build:
            tasks:
              - exec:
                  command: npm run build
